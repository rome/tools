---
title: Blog
layout: layouts/page.liquid
---

{% for post in collections.post %}
	<section class="blog">
		<article>
			<h1><a href="{{ post.url }}">{{ post.data.title }}</a></h1>
			<div class="info">
				<div>by <a href="{{ post.data.author_url }}">{{ post.data.author_name }}</a></div>
				<time datetime="{{ post.date }}"> {{ post.date | dateFormat }} </time>
			</div>
			<p>{{ post.templateContent | blogSummary }}</p>
			<p><a href="{{ post.url }}">Read more</a></p>

			<div class="tags">
				tags:
				{% for tag in post.data.tags %}
					{% if collections.tagList contains tag %}
						{% capture tagUrl %}/blog/tags/{{ tag }}/{% endcapture %}
						<a href="{{ tagUrl }}" class="tag">{{ tag }}</a>
					{% endif %}
				{% endfor %}
			</div>
		</article>
	</section>
{% endfor %}
