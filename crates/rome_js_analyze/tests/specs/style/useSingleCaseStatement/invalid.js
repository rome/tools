switch (foo) {
    // comment
    case false:
        let foo = '';
        foo;
}

switch (foo) {
    case false: // comment
        let foo = '';
        foo;
}

switch (foo) {
    case false
    /* comment */ :
        let foo = '';
        foo;
}

switch (foo) {
    case true:
    default:
        let foo = '';
        foo;
}

switch (foo) {
    // comment
    default:
        let foo = '';
        foo;
}

switch (foo) {
    default: // comment
        let foo = '';
        foo;
}

switch (foo) {
    default
    /* comment */ :
        let foo = '';
        foo;
}
