switch (foo) {
    case true:
    case false:
        'yes';
}

switch (foo) {
    case true: {
        // empty
    }
}

switch (foo) {
    case true:
}

switch (foo) {
    case true:
    default:
        'yes';
}

switch (foo) {
    default: {
        // empty
    }
}

switch (foo) {
    default:
}
