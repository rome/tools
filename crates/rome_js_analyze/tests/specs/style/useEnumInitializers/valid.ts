export enum Status {
	Open = 1,
	Close = 2,
}

export enum Color {
	Red = "Red",
	Green = "Green",
	Blue = "Blue",
}

export enum Mix {
	Str = "Str",
	Number = 0,
}

export declare enum Weather {
    Rainy,
    Sunny,
}

declare enum Weather2 {
    Rainy,
    Sunny,
}
