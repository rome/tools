function HighComplexity1() {
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };
    if (a) { func() } else { func() };

    return;
    afterReturn();
}
