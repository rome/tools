using a = b;
using c = d, e = _;
using [g] = h;
using [j]
= k;
await using l = m;
await using
n = o;
await
using
p = q;
// TODO: await using ([s] = t);
// TODO: await (using [u] = v);
using w = {};
using x = null;
using y = undefined;
using z = (foo, bar);
