using a = b;
using c = d, e = _;
using [g] = h;
using [j]
= k;
await using l = m;
await
using p = q;
await using[r];
await using ([s] = t);
await (using [u] = v);
using w = {};
using x = null;
using y = undefined;
using z = (foo, bar);
