---
source: crates/rome_js_formatter/tests/prettier_tests.rs
---

# Input

```js
let foo: (
    // comment
    any
);
let foo: (
    // comment
    null
);
let foo: (
    // comment
    this
);
let foo: (
    // comment
    number
);
let foo: (
    // comment
    void
);
let foo: (
    // comment
    boolean
);
let foo: (
    // comment
    bigint
);
let foo: (
    // comment
    symbol
);
let foo: (
    // comment
    string
);
let foo: (
    // comment
    never
);
let foo: (
    // comment
    object
);
let foo: (
    // comment
    undefined
);
let foo: (
    // comment
    unknown
);
```


# Prettier differences

```diff
--- Prettier
+++ Rome
@@ -1,26 +1,52 @@
-let foo: // comment
-any;
-let foo: // comment
-null;
-let foo: // comment
-this;
-let foo: // comment
-number;
-let foo: // comment
-void;
-let foo: // comment
-boolean;
-let foo: // comment
-bigint;
-let foo: // comment
-symbol;
-let foo: // comment
-string;
-let foo: // comment
-never;
-let foo: // comment
-object;
-let foo: // comment
-undefined;
-let foo: // comment
-unknown;
+let foo: (
+  // comment
+  any
+);
+let foo: (
+  // comment
+  null
+);
+let foo: (
+  // comment
+  this
+);
+let foo: (
+  // comment
+  number
+);
+let foo: (
+  // comment
+  void
+);
+let foo: (
+  // comment
+  boolean
+);
+let foo: (
+  // comment
+  bigint
+);
+let foo: (
+  // comment
+  symbol
+);
+let foo: (
+  // comment
+  string
+);
+let foo: (
+  // comment
+  never
+);
+let foo: (
+  // comment
+  object
+);
+let foo: (
+  // comment
+  undefined
+);
+let foo: (
+  // comment
+  unknown
+);
```

# Output

```js
let foo: (
  // comment
  any
);
let foo: (
  // comment
  null
);
let foo: (
  // comment
  this
);
let foo: (
  // comment
  number
);
let foo: (
  // comment
  void
);
let foo: (
  // comment
  boolean
);
let foo: (
  // comment
  bigint
);
let foo: (
  // comment
  symbol
);
let foo: (
  // comment
  string
);
let foo: (
  // comment
  never
);
let foo: (
  // comment
  object
);
let foo: (
  // comment
  undefined
);
let foo: (
  // comment
  unknown
);
```



