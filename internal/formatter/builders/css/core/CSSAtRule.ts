import {CSSAtRule} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSAtRule(builder: Builder, node: CSSAtRule): Token {
	throw new Error("unimplemented");
}
