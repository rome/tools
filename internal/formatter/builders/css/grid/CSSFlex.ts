import {CSSFlex} from "@internal/ast";
import {Builder, Token} from "@internal/formatter";

export default function CSSFlex(builder: Builder, node: CSSFlex): Token {
	throw new Error("unimplemented");
}
