import {TomlValueFloat} from "@internal/ast";
import {Builder, Token} from "@internal/formatter";

export default function TomlValueFloat(
	builder: Builder,
	node: TomlValueFloat,
): Token {
	throw new Error("unimplemented");
}
