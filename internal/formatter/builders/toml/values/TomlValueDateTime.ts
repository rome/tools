import {TomlValueDateTime} from "@internal/ast";
import {Builder, Token} from "@internal/formatter";

export default function TomlValueDateTime(
	builder: Builder,
	node: TomlValueDateTime,
): Token {
	throw new Error("unimplemented");
}
