import {TomlValueInlineTable} from "@internal/ast";
import {Builder, Token} from "@internal/formatter";

export default function TomlValueInlineTable(
	builder: Builder,
	node: TomlValueInlineTable,
): Token {
	throw new Error("unimplemented");
}
