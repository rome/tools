import {TomlValueString} from "@internal/ast";
import {Builder, Token} from "@internal/formatter";

export default function TomlValueString(
	builder: Builder,
	node: TomlValueString,
): Token {
	throw new Error("unimplemented");
}
