import {TomlValueArray} from "@internal/ast";
import {Builder, Token} from "@internal/formatter";

export default function TomlValueArray(
	builder: Builder,
	node: TomlValueArray,
): Token {
	throw new Error("unimplemented");
}
