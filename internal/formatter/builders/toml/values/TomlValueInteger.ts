import {TomlValueInteger} from "@internal/ast";
import {Builder, Token} from "@internal/formatter";

export default function TomlValueInteger(
	builder: Builder,
	node: TomlValueInteger,
): Token {
	throw new Error("unimplemented");
}
