import {TomlRoot} from "@internal/ast";
import {Builder, Token} from "@internal/formatter";

export default function TomlRoot(builder: Builder, node: TomlRoot): Token {
	throw new Error("unimplemented");
}
