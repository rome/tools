import {TomlKeyValue} from "@internal/ast";
import {Builder, Token} from "@internal/formatter";

export default function TomlKeyValue(
	builder: Builder,
	node: TomlKeyValue,
): Token {
	throw new Error("unimplemented");
}
