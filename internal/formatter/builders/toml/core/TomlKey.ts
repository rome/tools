import {TomlKey} from "@internal/ast";
import {Builder, Token} from "@internal/formatter";

export default function TomlKey(builder: Builder, node: TomlKey): Token {
	throw new Error("unimplemented");
}
