import {TomlText} from "@internal/ast";
import {Builder, Token} from "@internal/formatter";

export default function TomlText(builder: Builder, node: TomlText): Token {
	throw new Error("unimplemented");
}
