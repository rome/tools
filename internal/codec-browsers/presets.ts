// TODO we can inline the AST and save having to parse this every time
export const dead = "bb <= 10, op_mob <= 12.1, samsung 4";
export const modern = "last 2 versions, not dead";

export const maintainedNode = [10, 12, 14, 15];
