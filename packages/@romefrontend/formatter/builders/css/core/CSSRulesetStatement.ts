import {CSSRulesetStatement} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSRulesetStatement(
	builder: Builder,
	node: CSSRulesetStatement,
): Token {
	throw new Error("unimplemented");
}
