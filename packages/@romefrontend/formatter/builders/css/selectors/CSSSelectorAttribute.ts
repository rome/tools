import {CSSSelectorAttribute} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSSelectorAttribute(
	builder: Builder,
	node: CSSSelectorAttribute,
): Token {
	throw new Error("unimplemented");
}
