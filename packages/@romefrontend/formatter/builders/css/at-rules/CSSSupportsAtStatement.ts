import {CSSSupportsAtStatement} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSSupportsAtStatement(
	builder: Builder,
	node: CSSSupportsAtStatement,
): Token {
	throw new Error("unimplemented");
}
