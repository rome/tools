import {CSSCharSetAtStatement} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSCharSetAtStatement(
	builder: Builder,
	node: CSSCharSetAtStatement,
): Token {
	throw new Error("unimplemented");
}
