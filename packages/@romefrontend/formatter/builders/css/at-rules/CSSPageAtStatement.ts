import {CSSPageAtStatement} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSPageAtStatement(
	builder: Builder,
	node: CSSPageAtStatement,
): Token {
	throw new Error("unimplemented");
}
