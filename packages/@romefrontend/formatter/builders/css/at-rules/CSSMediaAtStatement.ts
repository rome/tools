import {CSSMediaAtStatement} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSMediaAtStatement(
	builder: Builder,
	node: CSSMediaAtStatement,
): Token {
	throw new Error("unimplemented");
}
