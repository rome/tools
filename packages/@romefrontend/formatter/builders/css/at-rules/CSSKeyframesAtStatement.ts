import {CSSKeyframesAtStatement} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSKeyframesAtStatement(
	builder: Builder,
	node: CSSKeyframesAtStatement,
): Token {
	throw new Error("unimplemented");
}
