import {CSSAngleType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSAngleType(
	builder: Builder,
	node: CSSAngleType,
): Token {
	throw new Error("unimplemented");
}
