import {CSSResolutionType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSResolutionType(
	builder: Builder,
	node: CSSResolutionType,
): Token {
	throw new Error("unimplemented");
}
