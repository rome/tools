import {CSSIntegerType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSIntegerType(
	builder: Builder,
	node: CSSIntegerType,
): Token {
	throw new Error("unimplemented");
}
