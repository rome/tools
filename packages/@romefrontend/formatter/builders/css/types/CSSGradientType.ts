import {CSSGradientType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSGradientType(
	builder: Builder,
	node: CSSGradientType,
): Token {
	throw new Error("unimplemented");
}
