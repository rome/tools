import {CSSIdentifierType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSIdentifierType(
	builder: Builder,
	node: CSSIdentifierType,
): Token {
	throw new Error("unimplemented");
}
