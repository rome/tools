import {CSSBasicShapeType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSBasicShapeType(
	builder: Builder,
	node: CSSBasicShapeType,
): Token {
	throw new Error("unimplemented");
}
