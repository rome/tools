import {CSSTimePercentageType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSTimePercentageType(
	builder: Builder,
	node: CSSTimePercentageType,
): Token {
	throw new Error("unimplemented");
}
