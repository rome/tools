import {CSSRatioType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSRatioType(
	builder: Builder,
	node: CSSRatioType,
): Token {
	throw new Error("unimplemented");
}
