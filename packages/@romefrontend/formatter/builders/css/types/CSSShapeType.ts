import {CSSShapeType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSShapeType(
	builder: Builder,
	node: CSSShapeType,
): Token {
	throw new Error("unimplemented");
}
