import {CSSLengthType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSLengthType(
	builder: Builder,
	node: CSSLengthType,
): Token {
	throw new Error("unimplemented");
}
