import {CSSFrequencyPercentageType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSFrequencyPercentageType(
	builder: Builder,
	node: CSSFrequencyPercentageType,
): Token {
	throw new Error("unimplemented");
}
