import {CSSImageType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSImageType(
	builder: Builder,
	node: CSSImageType,
): Token {
	throw new Error("unimplemented");
}
