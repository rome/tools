import {CSSStringType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSStringType(
	builder: Builder,
	node: CSSStringType,
): Token {
	throw new Error("unimplemented");
}
