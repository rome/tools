import {CSSTimeType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSTimeType(builder: Builder, node: CSSTimeType): Token {
	throw new Error("unimplemented");
}
