import {CSSBlendModeType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSBlendModeType(
	builder: Builder,
	node: CSSBlendModeType,
): Token {
	throw new Error("unimplemented");
}
