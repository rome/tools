import {CSSLengthPercentageType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSLengthPercentageType(
	builder: Builder,
	node: CSSLengthPercentageType,
): Token {
	throw new Error("unimplemented");
}
