import {CSSFrequencyType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSFrequencyType(
	builder: Builder,
	node: CSSFrequencyType,
): Token {
	throw new Error("unimplemented");
}
