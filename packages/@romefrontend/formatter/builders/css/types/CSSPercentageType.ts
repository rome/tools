import {CSSPercentageType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSPercentageType(
	builder: Builder,
	node: CSSPercentageType,
): Token {
	throw new Error("unimplemented");
}
