import {CSSDimensionType} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSDimensionType(
	builder: Builder,
	node: CSSDimensionType,
): Token {
	throw new Error("unimplemented");
}
