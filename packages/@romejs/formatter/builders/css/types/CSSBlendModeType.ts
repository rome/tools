import {CSSBlendModeType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSBlendModeType(
	builder: Builder,
	node: CSSBlendModeType,
): Token {
	throw new Error("unimplemented");
}
