import {CSSURLType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSURLType(builder: Builder, node: CSSURLType): Token {
	throw new Error("unimplemented");
}
