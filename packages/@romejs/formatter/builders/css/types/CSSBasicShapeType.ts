import {CSSBasicShapeType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSBasicShapeType(
	builder: Builder,
	node: CSSBasicShapeType,
): Token {
	throw new Error("unimplemented");
}
