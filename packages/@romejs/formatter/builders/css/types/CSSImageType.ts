import {CSSImageType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSImageType(
	builder: Builder,
	node: CSSImageType,
): Token {
	throw new Error("unimplemented");
}
