import {CSSNumberType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSNumberType(
	builder: Builder,
	node: CSSNumberType,
): Token {
	throw new Error("unimplemented");
}
