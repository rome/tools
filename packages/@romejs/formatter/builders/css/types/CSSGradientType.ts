import {CSSGradientType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSGradientType(
	builder: Builder,
	node: CSSGradientType,
): Token {
	throw new Error("unimplemented");
}
