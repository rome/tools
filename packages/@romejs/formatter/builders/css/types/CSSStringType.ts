import {CSSStringType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSStringType(
	builder: Builder,
	node: CSSStringType,
): Token {
	throw new Error("unimplemented");
}
