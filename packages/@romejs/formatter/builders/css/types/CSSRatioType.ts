import {CSSRatioType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSRatioType(
	builder: Builder,
	node: CSSRatioType,
): Token {
	throw new Error("unimplemented");
}
