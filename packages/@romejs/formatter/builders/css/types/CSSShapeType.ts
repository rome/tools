import {CSSShapeType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSShapeType(
	builder: Builder,
	node: CSSShapeType,
): Token {
	throw new Error("unimplemented");
}
