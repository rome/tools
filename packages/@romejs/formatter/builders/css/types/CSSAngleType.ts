import {CSSAngleType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSAngleType(
	builder: Builder,
	node: CSSAngleType,
): Token {
	throw new Error("unimplemented");
}
