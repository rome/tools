import {CSSIntegerType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSIntegerType(
	builder: Builder,
	node: CSSIntegerType,
): Token {
	throw new Error("unimplemented");
}
