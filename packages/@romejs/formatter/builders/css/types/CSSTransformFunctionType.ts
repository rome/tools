import {CSSTransformFunctionType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSTransformFunctionType(
	builder: Builder,
	node: CSSTransformFunctionType,
): Token {
	throw new Error("unimplemented");
}
