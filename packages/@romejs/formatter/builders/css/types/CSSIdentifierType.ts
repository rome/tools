import {CSSIdentifierType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSIdentifierType(
	builder: Builder,
	node: CSSIdentifierType,
): Token {
	throw new Error("unimplemented");
}
