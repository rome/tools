import {CSSFrequencyType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSFrequencyType(
	builder: Builder,
	node: CSSFrequencyType,
): Token {
	throw new Error("unimplemented");
}
