import {CSSAnglePercentageType} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSAnglePercentageType(
	builder: Builder,
	node: CSSAnglePercentageType,
): Token {
	throw new Error("unimplemented");
}
