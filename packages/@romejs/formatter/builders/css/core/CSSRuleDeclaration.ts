import {CSSRuleDeclaration} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSRuleDeclaration(
	builder: Builder,
	node: CSSRuleDeclaration,
): Token {
	throw new Error("unimplemented");
}
