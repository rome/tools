import {CSSSelectorCombinator} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSSelectorCombinator(
	builder: Builder,
	node: CSSSelectorCombinator,
): Token {
	throw new Error("unimplemented");
}
