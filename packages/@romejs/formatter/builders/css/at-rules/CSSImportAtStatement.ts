import {CSSImportAtStatement} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSImportAtStatement(
	builder: Builder,
	node: CSSImportAtStatement,
): Token {
	throw new Error("unimplemented");
}
