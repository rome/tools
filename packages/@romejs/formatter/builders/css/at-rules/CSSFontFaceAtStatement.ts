import {CSSFontFaceAtStatement} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSFontFaceAtStatement(
	builder: Builder,
	node: CSSFontFaceAtStatement,
): Token {
	throw new Error("unimplemented");
}
