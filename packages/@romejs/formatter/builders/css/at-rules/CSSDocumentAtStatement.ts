import {CSSDocumentAtStatement} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSDocumentAtStatement(
	builder: Builder,
	node: CSSDocumentAtStatement,
): Token {
	throw new Error("unimplemented");
}
