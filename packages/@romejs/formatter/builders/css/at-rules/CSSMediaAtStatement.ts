import {CSSMediaAtStatement} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSMediaAtStatement(
	builder: Builder,
	node: CSSMediaAtStatement,
): Token {
	throw new Error("unimplemented");
}
