import {CSSViewportAtStatement} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSViewportAtStatement(
	builder: Builder,
	node: CSSViewportAtStatement,
): Token {
	throw new Error("unimplemented");
}
