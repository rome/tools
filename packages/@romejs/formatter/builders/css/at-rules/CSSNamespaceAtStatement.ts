import {CSSNamespaceAtStatement} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSNamespaceAtStatement(
	builder: Builder,
	node: CSSNamespaceAtStatement,
): Token {
	throw new Error("unimplemented");
}
