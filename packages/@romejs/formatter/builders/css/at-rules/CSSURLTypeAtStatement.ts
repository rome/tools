import {CSSURLTypeAtStatement} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSURLTypeAtStatement(
	builder: Builder,
	node: CSSURLTypeAtStatement,
): Token {
	throw new Error("unimplemented");
}
