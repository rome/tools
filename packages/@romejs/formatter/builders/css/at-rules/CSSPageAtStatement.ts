import {CSSPageAtStatement} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSPageAtStatement(
	builder: Builder,
	node: CSSPageAtStatement,
): Token {
	throw new Error("unimplemented");
}
