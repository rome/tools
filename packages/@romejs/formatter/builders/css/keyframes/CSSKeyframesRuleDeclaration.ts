import {CSSKeyframesRuleDeclaration} from "@romejs/ast";
import {Builder, Token} from "@romejs/formatter";

export default function CSSKeyframesRuleDeclaration(
	builder: Builder,
	node: CSSKeyframesRuleDeclaration,
): Token {
	throw new Error("unimplemented");
}
