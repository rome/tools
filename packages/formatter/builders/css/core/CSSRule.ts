import {CSSRule} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSRule(builder: Builder, node: CSSRule): Token {
	throw new Error("unimplemented");
}
