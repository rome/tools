import {CSSBlock} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSBlock(builder: Builder, node: CSSBlock): Token {
	throw new Error("unimplemented");
}
