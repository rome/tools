import {CSSRaw} from "@romefrontend/ast";
import {Builder, Token} from "@romefrontend/formatter";

export default function CSSRaw(builder: Builder, node: CSSRaw): Token {
	throw new Error("unimplemented");
}
